# ls
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -F'

# Workpop
alias wp="cd $HOME/Programming/Workpop-Web"
alias wa="cd $HOME/Programming/Workpop-Analytics"

# Kubernetes
alias kc='kubectl'
alias uc='kubectl config use-context'

# Pandas
alias pandas="ipython -c 'import pandas as pd' -i"

# Chromebook helper
alias localip='ifconfig | ag inet | head -n 1'

# Hugo helper
alias hugoserver="hugo --bind=0.0.0.0 -b http://100.115.92.2:1313/ server -v --buildDrafts --disableFastRender"
